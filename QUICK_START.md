# دليل البدء السريع 🚀

## خطوات التشغيل السريعة

### 1. تثبيت المكتبات
```bash
pnpm install
```

### 2. إضافة الصور
أضف الصور التالية في مجلد `public/`:
- `hero-cobbler-workshop.jpg`
- `service-shoe-repair.jpg`
- `service-bag-repair.jpg`
- `service-traditional-shoes.jpg`

### 3. تشغيل المشروع
```bash
pnpm run dev
```

### 4. فتح المتصفح
افتح `http://localhost:5173` في المتصفح

## التخصيص السريع

### تغيير رقم الهاتف
افتح `src/lib/translations.ts` وعدّل:
```typescript
phone: {
  number: '+212 6XX XXX XXX', // ضع رقمك هنا
}
```

### تحديث موقع الخريطة
افتح `src/components/Map.tsx` وعدّل رابط Google Maps

## البناء للإنتاج
```bash
pnpm run build
```

الملفات الجاهزة ستكون في مجلد `dist/`

## ملاحظات مهمة

- تأكد من إضافة جميع الصور قبل البناء
- يمكنك تخصيص الألوان في `src/index.css`
- جميع النصوص قابلة للتعديل في `src/lib/translations.ts`

