# اسكافي البهجة - موقع ويب احترافي

موقع ويب من صفحة واحدة لمتجر "اسكافي البهجة" - متجر إسكافي تقليدي في المغرب.

🌐 **Live Demo**: [رابط الموقع المباشر](https://your-site.vercel.app)

📦 **Repository**: [GitHub](https://github.com/YOUR_USERNAME/eskify-al-bahja)

## 🛠️ التقنيات المستخدمة

- **React 18** - مكتبة JavaScript لبناء واجهات المستخدم
- **TypeScript** - لغة برمجة توفر نوع البيانات الثابت
- **Vite** - أداة بناء سريعة
- **Tailwind CSS** - إطار عمل CSS utility-first
- **Lucide React** - مكتبة أيقونات حديثة

## 📋 المتطلبات

- Node.js 18+ 
- pnpm (أو npm/yarn)

## 🚀 التثبيت والتشغيل

### 1. تثبيت المكتبات

```bash
pnpm install
```

### 2. تشغيل المشروع في وضع التطوير

```bash
pnpm run dev
```

سيتم فتح المشروع على `http://localhost:5173`

### 3. بناء المشروع للإنتاج

```bash
pnpm run build
```

### 4. معاينة البناء

```bash
pnpm run preview
```

## 📁 هيكل المشروع

```
├── index.html              # صفحة HTML الرئيسية
├── src/
│   ├── components/         # المكونات
│   │   ├── Hero.tsx        # قسم البطل
│   │   ├── Services.tsx    # قسم الخدمات
│   │   ├── Contact.tsx     # قسم التواصل
│   │   ├── Map.tsx         # قسم الخريطة
│   │   ├── Footer.tsx      # التذييل
│   │   └── LanguageSwitcher.tsx  # مبدل اللغة
│   ├── pages/
│   │   └── Index.tsx       # الصفحة الرئيسية
│   ├── lib/
│   │   ├── translations.ts # ملفات الترجمة
│   │   └── utils.ts        # دوال مساعدة
│   ├── index.css           # الأنماط الرئيسية
│   └── main.tsx            # نقطة الدخول
├── public/                 # الملفات الثابتة (الصور)
└── package.json
```

## 🎨 الميزات

### ✨ التصميم
- تصميم متجاوب (Responsive) يعمل على جميع الأجهزة
- لوحة ألوان مغربية تقليدية
- أنماط زخرفية مغربية في الخلفيات
- رسوم متحركة سلسة وتأثيرات hover

### 🌍 تعدد اللغات
- دعم العربية (RTL) والفرنسية (LTR)
- تبديل تلقائي لاتجاه الصفحة
- زر تبديل اللغة في أعلى اليسار

### 📱 الأقسام
1. **Hero Section** - قسم البطل مع صورة خلفية وCTA
2. **Services** - عرض الخدمات الثلاث (إصلاح أحذية، إصلاح حقائب، صناعة أحذية تقليدية)
3. **Contact** - معلومات التواصل (هاتف، ساعات العمل، الموقع)
4. **Map** - خريطة Google Maps للموقع
5. **Footer** - التذييل مع حقوق النشر

## 🖼️ الصور المطلوبة

يجب إضافة الصور التالية في مجلد `public/`:

- `hero-cobbler-workshop.jpg` - صورة ورشة الإسكافي للقسم الرئيسي
- `service-shoe-repair.jpg` - صورة إصلاح الأحذية
- `service-bag-repair.jpg` - صورة إصلاح الحقائب
- `service-traditional-shoes.jpg` - صورة الأحذية التقليدية المغربية

## ⚙️ التخصيص

### تغيير رقم الهاتف

قم بتعديل رقم الهاتف في ملف `src/lib/translations.ts`:

```typescript
phone: {
  number: '+212 6XX XXX XXX', // استبدل برقمك الفعلي
}
```

### تحديث موقع الخريطة

قم بتعديل إحداثيات الخريطة في ملف `src/components/Map.tsx`:

```typescript
const mapUrl = 'YOUR_GOOGLE_MAPS_EMBED_URL';
```

### تخصيص الألوان

يمكنك تعديل الألوان في ملف `src/index.css`:

```css
:root {
  --leather-brown: #8B5E3C;
  --light-gold: #D4AF37;
  --cream-beige: #F5E6CA;
  --dark-gray: #3C3C3C;
}
```

## 📝 الأوامر المتاحة

```bash
# التطوير
pnpm run dev

# البناء
pnpm run build

# فحص الأخطاء
pnpm run lint

# معاينة البناء
pnpm run preview
```

## 🌐 SEO

تم إضافة جميع meta tags اللازمة لتحسين محركات البحث:
- العنوان والوصف
- Open Graph tags للمشاركة على السوشيال ميديا
- الكلمات المفتاحية

## ♿ إمكانية الوصول

- استخدام semantic HTML
- تباين ألوان مناسب (WCAG AA)
- دعم قارئات الشاشة
- alt text للصور

## 📄 الترخيص

هذا المشروع مخصص لاستخدام "اسكافي البهجة".

## 👨‍💻 الدعم

لأي استفسارات أو مشاكل، يرجى التواصل مع فريق التطوير.

---

**صنع بحب ❤️ للمجتمع المغربي**

